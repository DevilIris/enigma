App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated config files (committed so the CI build, which stages assets with a
# plain copy instead of `cap sync`, finds them — the App target references them).
# App/App/capacitor.config.json
# App/App/config.xml
